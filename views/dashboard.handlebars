<section>
    <p>Name: {{user.name}}</p>
    <p>Email: {{user.email}}</p>
    <p>Phone: {{user.phone}}</p>
</section>
<section>
  {{#each user.reservations as |reservation| }}
    {{> reservation-card}}
  {{/each}}
</section>